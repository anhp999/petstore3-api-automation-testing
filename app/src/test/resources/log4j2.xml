<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <!-- Use ANSI colors via JAnsi; pattern wraps level with color codes -->
            <PatternLayout>
                <Pattern>%highlight{[%d{HH:mm:ss}] [%-5level] %c{1} - %msg%n}{FATAL=red blink, ERROR=red, WARN=yellow, INFO=green, DEBUG=cyan, TRACE=blue}</Pattern>
            </PatternLayout>
        </Console>

        <RollingFile name="FileLogger" fileName="test-output/automation.log"
                     filePattern="logs/test-%d{yyyy-MM-dd}.log">
            <PatternLayout>
                <Pattern>[%d{yyyy-MM-dd HH:mm:ss}] [%-5level] %c{1} - %msg%n</Pattern>
            </PatternLayout>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Root>
    </Loggers>
</Configuration>